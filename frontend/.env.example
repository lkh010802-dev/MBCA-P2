# Leave blank when the web server proxies /api to the backend.
# For split-domain deployment, set the public backend URL (no trailing slash).
VITE_API_BASE_URL=
VITE_KAKAO_MAP_KEY=
# Production uses the real DB-backed login by default. Use mock only for local demos.
VITE_AUTH_MODE=real
